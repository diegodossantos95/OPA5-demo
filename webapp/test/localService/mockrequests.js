sap.ui.define([
  'jquery.sap.global'
], function () {
  "use strict";

  var mockRequests = {
            /**
             * get requests
             * @return {array} Array of mock request
             */
    getRequests: function() {
      return [];
    }
  };

  return mockRequests;
},
    true);
